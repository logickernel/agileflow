agileflow:
  stage: .pre
  image: registry.logickernel.com/kernel/agileflow:0.6.42
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  script:
    - agileflow gitlab-ci
  artifacts:
    reports:
      dotenv: version.env