.agileflow:
  image: registry.logickernel.com/kernel/agileflow:0.6.43
  script:
    - agileflow gitlab-ci
  artifacts:
    reports:
      dotenv: VERSION

release_main:
  stage: build
  extends: .agileflow
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'