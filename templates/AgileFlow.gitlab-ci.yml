agileflow:
  stage: .pre
  image: registry.logickernel.com/kernel/agileflow:0.6.38
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - when: never
  script:
    - echo "VERSION=$(agileflow gitlab-ci)" | tee version.env
  artifacts:
    reports: { dotenv: version.env }