agileflow:
  stage: deploy
  script:
    - sed -i "s/AGILEFLOW_VERSION=.*/AGILEFLOW_VERSION=${CI_COMMIT_TAG}/" agileflow
    - ./agileflow tag --key ${AGILEFLOW_KEY} --debug
  only:
    - /^release\/[0-9]+\.[0-9]+$/
  tags:
    - agileflow
  artifacts:
    name: "agileflow_${CI_COMMIT_TAG}"
    paths:
      - agileflow
    when: on_success
    expire_in: "30 days"
  release:
    tag_name: "$CI_COMMIT_TAG"
    description: "$CI_COMMIT_TAG"
