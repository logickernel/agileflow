agileflow:
  image: node:20
  variables:
    GIT_DEPTH: 0
  script:
    - npm install -g --prefer-online @logickernel/agileflow
    - agileflow version
    - agileflow gitlab
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  tags:
    - agileflow