# .gitlab-ci.yml
tag_on_release_branch:
  image: registry.logickernel.com/kernel/agileflow:0.6.2
  only:
    - /^release\/[0-9]+\.[0-9]+$/
  script:
    - agileflow tag-version
  tags:
    - agileflow